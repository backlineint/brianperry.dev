---
import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import Stars from "../components/Stars.astro";
import FormattedDate from "../components/FormattedDate.astro";
import { SITE_TITLE } from "../consts";

interface Props {
  title: string;
  description?: string;
  pubDate: Date;
  updatedDate?: string;
  heroImage?: string;
  ep?: number;
  tag?: string;
}

const { title, description, pubDate, updatedDate, heroImage, ep, tag } =
  Astro.props;
const epLabel = ep ? `EP ${String(ep).padStart(3, "0")}` : null;
const duo = ep ? (ep % 6) + 1 : 6;
---

<html lang="en">
  <head>
    <BaseHead
      title={`${title} - ${SITE_TITLE}`}
      description={description ? description : "brianperry.dev"}
      image={heroImage}
    />
  </head>

  <body>
    <Stars />
    <Header />
    <main>
      <article class="article">
        <a href="/posts" class="crumb">
          ← all posts{epLabel ? ` / ${epLabel.toLowerCase()}` : ""}
        </a>
        <div class="pmeta">
          {epLabel && <span class="badge red">★ {epLabel}</span>}
          <span class="mono date">
            <FormattedDate date={new Date(pubDate)} />
          </span>
        </div>
        <h1>{title}</h1>
        {
          updatedDate && (
            <p class="last-updated-on">
              Last updated on <FormattedDate date={new Date(updatedDate)} />
            </p>
          )
        }

        {
          heroImage ? (
            <figure class="phero">
              <img src={heroImage} alt="" />
              {tag && <figcaption class="tlbl">{tag}</figcaption>}
            </figure>
          ) : (
            <figure class:list={["phero", "ph-gradient", `duo-${duo}`]}>
              {tag && <figcaption class="tlbl">{tag}</figcaption>}
            </figure>
          )
        }

        <div class="prose body">
          <slot />
        </div>
      </article>
    </main>
    <Footer />
    <style>
      .article {
        max-width: 720px;
        margin: 0 auto;
        padding-top: 8px;
      }
      .crumb {
        font-family: "JetBrains Mono", monospace;
        font-size: 12px;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: var(--faint);
        text-decoration: none;
        display: inline-block;
      }
      .crumb:hover {
        color: var(--cyan);
      }
      .pmeta {
        display: flex;
        gap: 10px;
        align-items: center;
        margin: 26px 0 0;
        justify-content: center;
      }
      .pmeta .date {
        color: var(--gold);
        font-size: 12px;
        letter-spacing: 0.08em;
      }
      .article h1 {
        font-size: 54px;
        line-height: 1;
        letter-spacing: -0.03em;
        margin: 18px 0 0;
        text-align: center;
        text-transform: uppercase;
      }
      .last-updated-on {
        text-align: center;
        font-style: italic;
        color: var(--muted);
        margin: 12px 0 0;
        font-size: 14px;
      }
      .phero {
        margin: 28px 0 0;
        border: 2px solid var(--line);
        border-radius: 18px;
        overflow: hidden;
        aspect-ratio: 16 / 8;
        box-shadow: 6px 6px 0 rgba(0, 0, 0, 0.3);
        position: relative;
      }
      .phero img {
        position: absolute;
        inset: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
        border: 0;
        border-radius: 0;
        box-shadow: none;
      }
      .phero .tlbl {
        position: absolute;
        right: 12px;
        bottom: 11px;
        font-family: "JetBrains Mono", monospace;
        font-size: 11px;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        font-weight: 700;
        color: rgba(20, 12, 40, 0.7);
      }
      .phero:not(.ph-gradient) .tlbl {
        color: rgba(255, 255, 255, 0.85);
        text-shadow: 0 1px 4px rgba(0, 0, 0, 0.6);
      }
      .body {
        margin-top: 34px;
      }
      @media (max-width: 720px) {
        .article h1 {
          font-size: 38px;
        }
      }
    </style>
  </body>
</html>
